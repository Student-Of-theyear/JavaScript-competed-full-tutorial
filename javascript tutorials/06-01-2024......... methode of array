// Array method

//3->toString() : converts arrya to stirng

// let marks = [97, 86, 36, 54];
// console.log(marks);
// console.log(marks.toString());

//4->Concat() : joins multiple arrays & returns result->origenal arry not change new result return krke deta hai

// let superHeroes = ["spiderman", "ironman", "saktiman", "superman"];
// let moveHeroes = ["salmankhan", "ajaydevgan", "sanjaydat"];
// let heroes = superHeroes.concat(moveHeroes);
//  console.log(heroes); 

//5-> unshift() : add to start........? =>(do work like push)

// let superHeroes = ["thor", "spiderman", "ironman"];
//  superHeroes.unshift("antman");

//6->shift() : delete from start & return........? => (pop ki tarah kaam krta hai)

// let superHeroes = ["thor", "spiderman", "ironman"];
// let value = superHeroes.shift();
// console.log("deleted", value);

//7-> slice() : returns a piece of the array => slice(startIdx,endIdx)

// let marvelHeroes = ["thro", "spiderman", "ironman", "antman", "dr.strange"];
// console.log(marvelHeroes);
// console.log(marvelHeroes.slice(1,3)); //slice=> kbhi bhi last value print nhi krta (1,3) only 1,2=> print krega

//8->splice() : change original array (add remove,replace)
// splice(startIdx,delCount,newEl1...)
//splice ko elements aad delete and replace krene ke liye use kiya jata hai

// let array = [1, 2, 3, 4, 5, 6, 7,];
// array.splice(2, 3, 102, 103);

// Add element
// let array = [1, 2, 3, 4, 5, 6, 7,];
// array.splice(2, 0, 101);

//Delete element
// let array = [1, 2, 3, 4, 5, 6, 7,];
// array.splice(3, 1,);

//Replace element
// let array = [1, 2, 3, 4, 5, 6, 7,];
// array.splice(1, 0, 202,203);